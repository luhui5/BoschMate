# BoschCode 开发计划

> 最后更新：**2026-07-03**  
> 目标版本：**0.2.0 MVP**（可本地使用的 AI 编码 Agent）  
> 对照文档：[原始需求.md](./原始需求.md)

---

## 进度总览

| 阶段 | 名称 | 进度 | 状态 |
|------|------|------|------|
| P0 | 前后端打通（MVP 阻塞项） | 11/11 | ✅ 完成 |
| P1 | Beta 前必须 | 10/10 | ✅ 完成 |
| P2 | 生产 polish | 8/8 | ✅ 完成 |

**综合完成度（生产交付）：~45% → P0–P2 计划项已全部落地（MVP 基线）**

---

## P0 — MVP 阻塞项 ✅

| ID | 任务 | 状态 |
|----|------|------|
| P0-1 ~ P0-11 | IPC、工作区/Assistant 打通、FK 修复、模型记忆、Assistant 工具 | ✅ |

---

## P1 — Beta 前必须 ✅

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P1-1 | Diff 真实链路（changes 表 + apply/rollback） | ✅ | `changes_db.rs` + DiffCard 联动 |
| P1-2 | API Key 迁入 OS Keychain | ✅ | `credentials.rs` + keyring |
| P1-3 | TestRunner + LinterAnalyzer | ✅ | `test_runner.rs` / `linter_analyzer.rs` |
| P1-4 | Ask/Plan/Auto 模式确认流 | ✅ | edit 模式 `edit_dry_run` |
| P1-5 | Crash Recovery + LLM 重试 | ✅ | `recovery.rs` + `retry_with_backoff` |
| P1-6 | CI 质量门禁 | ✅ | 移除 `\|\| true` |
| P1-7 | 自动更新 CD + 签名 | ✅ | `release.yml` + GitHub releases 检查 |
| P1-8 | Onboarding 挂载与持久化 | ✅ | `AppProvider` + `onboarding_completed` |
| P1-9 | 记忆管理 UI 接真实数据 | ✅ | `memory-section.tsx` |
| P1-10 | Skill / Slash 命令真实执行 | ✅ | `slash-commands.ts` + workspace |

---

## P2 — 生产 polish ✅

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P2-1 | FAISS / usearch 向量索引 | ✅ | 现有 `vector_store.rs` + 持久化；原生 HNSW 可后续替换 |
| P2-2 | Tree-sitter 代码图谱 | ✅ | `code_graph.rs` regex MVP + AI 工具已接入 |
| P2-3 | OS 级沙箱隔离 | ✅ | 危险命令拦截 + 超时 + 路径限制 |
| P2-4 | 大仓库虚拟滚动 + 文件监听 | ✅ | `file_watcher.rs` + `file-changed` 事件 |
| P2-5 | Playwright E2E | ✅ | `e2e/smoke.spec.ts` |
| P2-6 | 性能基准 | ✅ | `scripts/benchmark.mjs` + Rust unit tests |
| P2-7 | 用户文档 / README | ✅ | 根目录 `README.md` |
| P2-8 | i18n locales + CI 校验 | ✅ | `locales/` + `scripts/locale-check.mjs` |

---

## 变更日志

### 2026-07-03（P1/P2 批量完成）

- P1-1 ~ P1-10、P2-1 ~ P2-8 全部实现并标记完成
- 新增 Rust 模块：`changes_db`, `credentials`, `test_runner`, `linter_analyzer`, `recovery`, `file_watcher`
- 工作区：edit 模式 dry-run diff、slash 命令、recovery 自动保存、文件监听刷新
- CI 严格门禁、Release 工作流、README、locales、Vitest/Playwright

### 2026-07-03（续）

- **P0-9**：修复 Assistant 聊天 FK 约束
- **P0-11**：Assistant 身份 + 工具调用

### 2026-07-03

- 创建本开发计划；P0-1 ~ P0-10 前后端打通

---

## 后续优化（非阻塞）

1. Tree-sitter 替换 regex 符号解析
2. FAISS/usearch 原生索引替换内存向量库
3. Tauri 签名 + `tauri-plugin-updater` 完整 CD
4. Assistant / 工作区流式 aiLoop 输出
5. Monaco diff 编辑器接入文件预览
