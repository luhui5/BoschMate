# BoschCode 开发计划

> 最后更新：**2026-07-03**  
> 目标版本：**0.2.0 MVP**（可本地使用的 AI 编码 Agent）  
> 对照文档：[原始需求.md](./原始需求.md)

---

## 进度总览

| 阶段 | 名称 | 进度 | 状态 |
|------|------|------|------|
| P0 | 前后端打通（MVP 阻塞项） | 4/8 | 🔄 进行中 |
| P1 | Beta 前必须 | 0/10 | ⏳ 待开始 |
| P2 | 生产 polish | 0/8 | ⏳ 待开始 |

**综合完成度（生产交付）：~30% → 目标 P0 完成后 ~45%**

---

## P0 — MVP 阻塞项

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P0-1 | 创建 IPC 类型映射层（snake_case ↔ camelCase） | ✅ 完成 | `lib/ipc-mapper.ts` |
| P0-2 | `tauri-api` 补全 `aiLoopChat` 等缺失接口 | ✅ 完成 | |
| P0-3 | 新建/打开项目接入 `create_project` + `pickFolder` | ✅ 完成 | |
| P0-4 | 工作区加载真实项目（按 ID 从 DB 读取） | ✅ 完成 | |
| P0-5 | 工作区聊天接入 `streamChat` / `aiLoopChat` | ✅ 完成 | ask/plan→stream；edit/auto→loop |
| P0-6 | 文件树接入 `list_directory` | ✅ 完成 | |
| P0-7 | Git 面板接入 `git_status` / `git_commit` | ✅ 完成 | |
| P0-8 | 默认模型种子数据（首次可用） | ✅ 完成 | Ollama 本地默认 |
| P0-9 | Assistant 会话持久化（修复 FK 约束） | ✅ 完成 | `__assistant__` 虚拟项目 + DB 同步 |
| P0-10 | 模型选择记忆（上次选用 + 最常用默认） | ✅ 完成 | `bc-last-model-id` / `bc-model-usage` |
| P0-11 | Assistant 身份 + 工具调用（绑定文件夹后） | ✅ 完成 | system prompt + ai_loop + OpenAI tools 格式修复 |

---

## P1 — Beta 前必须

| ID | 任务 | 状态 |
|----|------|------|
| P1-1 | Diff 真实链路（changes 表 + apply/rollback） | ⏳ |
| P1-2 | API Key 迁入 OS Keychain | ⏳ |
| P1-3 | TestRunner + LinterAnalyzer | ⏳ |
| P1-4 | Ask/Plan/Auto 模式确认流 | ⏳ |
| P1-5 | Crash Recovery + LLM 重试 | ⏳ |
| P1-6 | CI 质量门禁（去掉 `\|\| true`） | ⏳ |
| P1-7 | 自动更新 CD + 签名 | ⏳ |
| P1-8 | Onboarding 挂载与持久化 | ⏳ |
| P1-9 | 记忆管理 UI 接真实数据 | ⏳ |
| P1-10 | Skill / Slash 命令真实执行 | ⏳ |

---

## P2 — 生产 polish

| ID | 任务 | 状态 |
|----|------|------|
| P2-1 | FAISS / usearch 向量索引 | ⏳ |
| P2-2 | Tree-sitter 代码图谱 | ⏳ |
| P2-3 | OS 级沙箱隔离 | ⏳ |
| P2-4 | 大仓库虚拟滚动 + 文件监听 | ⏳ |
| P2-5 | Playwright E2E（6 条路径） | ⏳ |
| P2-6 | 性能基准与达标验证 | ⏳ |
| P2-7 | 用户文档 / README | ⏳ |
| P2-8 | i18n locales 目录 + CI 校验 | ⏳ |

---

## 变更日志

### 2026-07-03（续）

- **P0-9**：修复 Assistant 聊天 `FOREIGN KEY constraint failed`
  - 原因：前端会话 ID 未写入 SQLite，`stream_chat` 保存回复时外键失败
  - 新增 `__assistant__` 虚拟项目；Assistant 会话创建/加载/发送与 DB 同步

### 2026-07-03

- 创建本开发计划文档
- **P0-1 ~ P0-8**：前后端打通第一批
  - 新增 `lib/ipc-mapper.ts` 统一 Rust/前端类型转换
  - `tauri-api` 增加 `aiLoopChat`、`openProject` 封装
  - 主页新建项目走 `create_project`
  - 工作区按 projectId 加载真实项目、会话、聊天、文件树、Git
  - 默认 Ollama 模型种子

---

## 下一步（P1 优先）

1. Diff 卡片与 `edit_file` / `rollback_edit` 后端联动
2. `changes` 表写入与 session 维度变更追踪
3. CI 流水线改为严格阻断
