下面将你前面所有讨论内容进行结构化汇总，形成一个更清晰、更可执行的产品方案，覆盖你要求的五个部分：**概要、页面与交互、实现架构及逻辑、技术栈、开发步骤**，并已补齐关键的技术选型结论和非功能性需求。

---

## 1 概要

YourMate 是一个本地运行的 AI Coding Agent 工具，目标是成为开发者的"本地智能助手"，具备长期记忆、自动压缩记忆、自动化代码修改、Git 集成、测试执行、计划生成等能力。它以聊天为核心交互方式，通过不同 Agent 模式实现从解释到自动化执行的完整开发流程。

### 核心价值

- **本地运行、安全可控**：所有代码修改、命令执行、测试运行都在本地项目内完成。
- **多 Agent 模式**：Bosch Assistant、Ask before edits、Plan、Edit automation。
- **可视化开发辅助**：文件树、变更视图、Git 视图、可视化 diff。
- **长期记忆系统**：自动压缩、向量检索、事实记忆、交互记忆、行为记忆。
- **AI Loop 自动化**：理解 → 计划 → 执行 → 验证 → 提交 → 记忆更新。

### 目标用户

- 个人开发者 / 小型团队（1-10 人）
- 关注代码隐私和安全，不希望代码离开本地
- 希望 AI 深度理解项目上下文和开发习惯

### 1.3 非功能性需求

#### 1.3.1 性能指标

| 维度 | 接受阈值（低于此值不可发布） | 目标值 | 理想值 |
|------|---------------------------|--------|--------|
| **冷启动时间** | < 5s | < 3s | < 1.5s |
| **热启动时间**（后台驻留恢复） | < 2s | < 1s | < 500ms |
| **内存占用（空闲）** | < 300MB | < 200MB | < 100MB |
| **内存占用（活跃项目 10 万文件）** | < 1.5GB | < 1GB | < 600MB |
| **安装包体积** | < 80MB | < 50MB | < 20MB |
| **文件树首次加载（10 万文件）** | < 5s | < 2s | < 1s |
| **文件树展开/折叠** | < 200ms | < 100ms | < 50ms |
| **流式聊天首 token** | < 2s | < 1s | < 500ms |
| **FAISS 检索（1 万条）** | < 100ms | < 50ms | < 20ms |
| **Diff 渲染（5000 行）** | < 1s | < 500ms | < 200ms |
| **CPU 占用（空闲）** | < 5% | < 2% | < 1% |

#### 1.3.2 安全底线

##### 威胁模型

```
信任边界：
  ┌──────────┐       ┌──────────────┐       ┌──────────┐
  │ 用户输入  │ ──→  │  YourMate   │ ──→   │ 项目文件  │
  │ (聊天/Skill)│     │  (可信进程)   │       │ 系统      │
  └──────────┘       └──────┬───────┘       └──────────┘
                            │
                     ┌──────▼───────┐
                     │  LLM 后端     │
                     │  (不可信边界)  │
                     └──────────────┘
```

**核心原则**：
- LLM 返回的内容视为**不可信输入**：输出需校验（diff 合理性、命令合法性、路径在项目内）
- 命令执行必须沙箱隔离：网络访问默认拦截，文件写入仅限项目目录
- Skill 插件为最小权限：每个 Skill 声明所需权限，用户显式授权

##### 安全要求明细

| 领域 | 要求 | 验证方式 |
|------|------|----------|
| **LLM 输出校验** | 生成的路径必须在项目目录内（`..`、符号链接、绝对路径均拒绝） | 自动化测试 + 运行时断言 |
| **命令沙箱** | 默认阻止网络访问；需用户配置白名单（如 `*.npmjs.org`）后才放行 | 集成测试 |
| **文件写入** | Auto 模式下单次会话写入 > 50 个文件时弹确认；禁止修改 `.git` 目录 | 运行时检查 |
| **密钥存储** | API Key、记忆加密密钥使用 OS 原生密钥链（Windows Credential Manager / macOS Keychain / Linux Secret Service） | 安全审计 |
| **数据加密** | 敏感记忆条目使用 AES-256-GCM 加密，密钥派生自用户口令（Argon2id） | 单元测试 |
| **网络通信** | 所有 LLM API 调用强制 HTTPS；证书校验不可跳过 | 代码审查 |
| **依赖安全** | `cargo audit` + `npm audit` 入 CI；禁止使用含有已知 CVE 的依赖 | CI 流水线 |
| **输入校验** | 用户输入长度限制（消息 < 64KB，粘贴文件 < 10MB），防止 DoS | 单元测试 |
| **更新安全** | 自动更新必须验证签名（ed25519），更新包哈希校验 | 设计审查 |

##### 平台安全特性

| 平台 | 沙箱机制 | 备注 |
|------|---------|------|
| **Windows** | Job Objects + 受限 Token | 移除 SeShutdownPrivilege 等敏感权限 |
| **macOS** | App Sandbox + seatbelt | 利用 Tauri 内置沙箱能力 |
| **Linux** | seccomp-bpf + user namespaces | 最小系统调用白名单 |

#### 1.3.3 可靠性与容灾

| 场景 | 目标行为 | 恢复策略 |
|------|---------|----------|
| **进程崩溃** | 未保存的聊天内容写入 recovery 文件 | 下次启动时提示恢复 |
| **LLM 连接中断** | 显示错误提示，不丢失已输入内容 | 自动重试 3 次（指数退避 1s/2s/4s） |
| **磁盘空间不足** | 在写入前检查可用空间（< 100MB 时警告） | 暂停自动操作，提示用户清理 |
| **SQLite 损坏** | 检测到损坏时弹修复向导 | 从 WAL 日志恢复 → 重建索引 → 提示备份还原 |
| **断电/强制关闭** | SQLite WAL 模式保证不丢数据 | 下次启动自动回放 WAL |
| **长时间运行 > 24h** | 定期 GC 未使用的缓存，定时 FAISS 索引优化 | 内存超过阈值时主动释放冷数据 |

#### 1.3.4 兼容性矩阵

| 平台 | 最低版本 | 推荐版本 | 架构 |
|------|---------|---------|------|
| **Windows** | Windows 10 21H2 | Windows 11 | x86_64, ARM64 |
| **macOS** | macOS 13 Ventura | macOS 15 | x86_64, ARM64 (Apple Silicon 原生) |
| **Linux** | Ubuntu 22.04 | Ubuntu 24.04 | x86_64 |

#### 1.3.5 可观测性

| 维度 | MVP 要求 | 长期目标 |
|------|---------|----------|
| **日志** | 结构化日志（tracing crate），按日轮转 | 可配置日志级别，远程导出 |
| **遥测** | 仅收集崩溃报告 + 匿名使用统计；默认关闭，需用户主动开启 | 性能分析面板（仅本地） |
| **审计** | 所有命令执行、文件写入、Git 操作记录到 audit_log 表 | 审计日志可视化面板 |

---

## 2 页面与交互

### 2.1 主页

- 顶部：搜索栏、快速新建项目、连接 SSH。
- 中间：项目卡片列表（项目名、语言栈、上次打开时间、最近聊天摘要、CI 状态）。
- 底部：本地与 SSH 历史记录（可置顶、删除、分页）。
- 点击项目：进入聊天页面并加载项目上下文与长期记忆。

### 2.2 项目页面布局

- **左侧栏**：项目切换、会话列表、项目记忆、笔记。
- **中间聊天区**：消息流、输入框、模式切换、截图粘贴分析。
- **右侧栏**（三种视图）：
  - 文件树（预览、跳转、右键操作）
  - 本次变更（diff、回滚）
  - Git 变更列表（staged/unstaged、提交、分支、PR 草稿）
- **底部工具栏**：
  - 上传（+）
  - 设置与 Skill（/）
  - Agent 模式切换
  - 发送按钮
  - 快捷操作（测试、格式化、lint）

### 2.3 设置页面

设置页面按优先级从上到下排列，左侧为分类导航，右侧为内容区。

#### 2.3.1 外观（置顶）

- **语言 (Language)**：下拉选择，支持 **English** / **简体中文**。切换后全局即时生效，无需重启。语言文件独立存放于 `locales/` 目录（详见 [8.3 国际化](#83-国际化-i18n)）。
- **主题**：暗色 / 亮色 / 跟随系统，实时预览
- **字体大小**：小 / 中 / 大（影响聊天和代码展示区）
- **编辑器字体**：等宽字体选择，字号独立调节

#### 2.3.2 模型配置

- 本地模型（Ollama/LM Studio 地址）、远程 API Key、模型参数（temperature、max_tokens）
- 模型连接测试按钮

#### 2.3.3 快捷键

- 全局快捷键自定义（发送消息、模式切换、打开文件树、切换视图等）

#### 2.3.4 记忆管理

- 查看 / 搜索 / 编辑 / 删除记忆条目（审查界面）

#### 2.3.5 隐私与安全

- 加密密钥管理、遥测开关、联网权限白名单

#### 2.3.6 关于与更新

- 当前版本号与更新通道（stable / beta）
- [检查更新] 按钮 → 详见 [2.6 更新功能](#26-更新功能)

### 2.4 聊天交互增强

- 可视化 diff 卡片（支持部分采纳、回滚）
- 会话固定、合并、导出为 Plan 文档
- 历史命令补全（如 `/run-tests`）
- 文件引用自动跳转
- 截图粘贴分析

### 2.5 状态覆盖（每页面均需覆盖）

| 状态 | 说明 |
|------|------|
| **Loading** | 骨架屏 / 进度条，避免白屏 |
| **Empty** | 引导文案 + CTA 按钮（如"打开第一个项目"） |
| **Error** | 错误信息 + 重试 / 降级操作 |
| **Large Repo** | 文件树虚拟滚动，排除 node_modules/.git 等目录 |

### 2.6 更新功能

#### 2.6.1 更新检测机制

| 触发方式 | 说明 |
|----------|------|
| **启动时自动检查** | 应用启动后 5s 内后台静默检查，有更新时弹出通知 |
| **定时检查** | 每 6 小时后台轮询一次 |
| **手动检查** | 设置页 → 关于与更新 → [检查更新] 按钮 |
| **更新通道** | stable（默认）/ beta（用户可选） |

后台检查到更新后，如果用户未主动触发，在主窗口右下角弹出更新提示（非模态，可忽略）。

#### 2.6.2 更新弹窗

```
有新版本时弹出：

  ┌───────────────────────────────────────┐
  │   🎉 发现新版本                        │
  │                                       │
  │   当前版本: v0.3.1                     │
  │   最新版本: v0.4.0                     │
  │                                       │
  │   更新内容:                             │
  │   · 新增 Ask before edits 完整流程      │
  │   · 修复大仓库文件树卡顿问题             │
  │   · 优化 FAISS 索引重建性能             │
  │   · [查看完整 Changelog]               │
  │                                       │
  │   [稍后提醒]  [跳过此版本]  [立即更新]     │
  └───────────────────────────────────────┘
```

交互说明：

| 操作 | 行为 |
|------|------|
| **[立即更新]** | 关闭弹窗 → 进入确认弹窗 |
| **[稍后提醒]** | 关闭弹窗，4 小时后再次提示 |
| **[跳过此版本]** | 关闭弹窗，此版本号不再提示，直至下一版本发布 |
| **[查看完整 Changelog]** | 浏览器打开 GitHub Release 页面 |

#### 2.6.3 确认弹窗（下载前）

```
用户点击 [立即更新] 后：

  ┌───────────────────────────────────────┐
  │   ⬇️ 准备更新                          │
  │                                       │
  │   更新版本: v0.4.0                     │
  │   下载大小: ~18MB                      │
  │                                       │
  │   ⚠ 更新过程中应用将重启               │
  │   请确保所有工作已保存                   │
  │                                       │
  │   [取消]  [保存工作并更新]              │
  └───────────────────────────────────────┘
```

点击 [保存工作并更新] 后：
1. 自动触发 recovery 文件写入（保护当前会话）
2. 开始下载更新包

#### 2.6.4 下载与安装进度

下载开始后，状态栏显示进度指示器，点击可展开详情：

```
状态栏进度指示器（可点击展开）：

  ┌───────────────────────────────────────┐
  │   ⬇️ 正在下载更新... 45%               │
  │   ████████████░░░░░░░░░░░  8.1/18 MB  │
  │                                       │
  │   速度: 2.3 MB/s  剩余时间: ~4s         │
  │                                       │
  │   [后台下载]                           │
  │   (点击后台下载后关闭此面板，状态栏保留    │
  │    迷你进度条)                         │
  └───────────────────────────────────────┘
```

下载完成后：

```
  ┌───────────────────────────────────────┐
  │   ✅ 下载完成                          │
  │                                       │
  │   v0.4.0 已下载完毕，签名验证通过       │
  │                                       │
  │   [稍后安装]  [立即重启安装]             │
  └───────────────────────────────────────┘
```

| 操作 | 行为 |
|------|------|
| **[立即重启安装]** | 关闭应用 → 启动安装程序 → 替换二进制 → 重新启动 |
| **[稍后安装]** | 下次关闭应用时自动安装 |

#### 2.6.5 安装与回滚

- 安装前校验更新包签名（ed25519）和文件哈希
- 安装失败时自动回滚至上一版本，弹出错误报告
- 旧版本二进制备份保留最近 2 个版本，用户可在设置中手动回退

```
安装失败时：

  ┌───────────────────────────────────────┐
  │   ❌ 更新失败                          │
  │                                       │
  │   错误: 签名校验失败                     │
  │                                       │
  │   已自动回滚至 v0.3.1                   │
  │                                       │
  │   [查看详情]  [重试]  [提交反馈]         │
  └───────────────────────────────────────┘
```

#### 2.6.6 与 CD 流水线的衔接

更新包由 CD Pipeline（见 [6.4 CI/CD 流水线](#64-cicd-流水线)）自动构建和签名，发布到更新服务器。客户端通过以下 API 检查更新：

```
GET https://updates.YourMate.dev/api/check?channel=stable&version=0.3.1&platform=win-x64

Response:
{
  "latest_version": "0.4.0",
  "download_url": "https://.../YourMate_0.4.0_x64.msi",
  "signature_url": "https://.../YourMate_0.4.0_x64.msi.sig",
  "size_bytes": 18874368,
  "changelog": "...",
  "min_previous_version": "0.2.0"
}
```

---

## 3 实现架构及逻辑

### 3.1 整体架构分层

```
┌─────────────────────────────────────────────────────────────┐
│  UI 层 (React + TypeScript + Monaco Editor)                 │
│  聊天 · 文件树 · Diff · Git 视图 · 模式切换 · 记忆审查      │
├─────────────────────────────────────────────────────────────┤
│  IPC 层 (Tauri invoke / event)                              │
├─────────────────────────────────────────────────────────────┤
│  Core 服务层 (Rust)                                         │
│  ┌──────────┬──────────┬───────────┬──────────┬──────────┐  │
│  │ FileOps  │ GitMgr   │ MemoryMgr │ SkillMgr │ Sandbox  │  │
│  │ 文件系统  │ Git 操作  │ SQLite +  │ 插件管理  │ 命令沙箱  │  │
│  │          │          │ FAISS     │          │          │  │
│  └──────────┴──────────┴───────────┴──────────┴──────────┘  │
├─────────────────────────────────────────────────────────────┤
│  AI 编排层 (Rust)                                           │
│  ┌──────────┬──────────┬───────────┬──────────────┐        │
│  │ Retriever│ Planner  │ Executor  │ Compressor   │        │
│  │ 上下文检索│ 计划生成  │ 工具编排   │ 记忆压缩     │        │
│  └──────────┴──────────┴───────────┴──────────────┘        │
├─────────────────────────────────────────────────────────────┤
│  AI 适配层 (Rust)                                           │
│  ┌──────────────────┬──────────────────┐                   │
│  │ 远程 API Client   │ 本地 LLM Client   │                   │
│  │ (Anthropic/OpenAI)│ (Ollama/LMStudio) │                   │
│  └──────────────────┴──────────────────┘                   │
├─────────────────────────────────────────────────────────────┤
│  工具层 (Rust trait 插件化)                                  │
│  ┌─────────┬─────────┬──────────┬──────────┬──────────┐    │
│  │CodeEdtr │DiffView │TestRunner│Linter   │FileSearch│    │
│  │补丁应用  │变更展示  │测试执行   │代码检查  │全文搜索   │    │
│  └─────────┴─────────┴──────────┴──────────┴──────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 AI Loop 逻辑流程

所有 Agent 模式共享以下基础流程：

1. **理解** — Retriever 检索上下文 → 模型分析 → 生成需求摘要
2. **计划** — PlanGenerator → 生成 Plan 文档或执行计划
3. **确认** — Ask before edits 模式需要用户确认；Auto 模式自动确认（可配置）
4. **执行** — CodeEditor 应用补丁（dry-run → apply）→ DiffViewer 展示变更 → SandboxExecutor 执行命令 → TestRunner 运行测试 → LinterAnalyzer 检查问题
5. **提交** — GitManager 提交或生成 PR 草稿
6. **记忆更新** — MemoryCompressor → 写入长期记忆

### 3.3 长期记忆逻辑

- **数据结构**：事实记忆、交互记忆、行为记忆、计划记忆（详见 [4.3 Schema 设计](#43-数据库-schema-设计)）
- **存储**：SQLite + 文件系统 + FAISS 向量索引
- **压缩策略**：定期聚合相似条目，生成摘要；版本链追踪压缩来源
- **检索策略**：语义相似度（FAISS）+ 时间衰减权重 + 重要性加权
- **隐私**：AES-256-GCM 加密敏感记忆，密钥由用户持有
- **审查界面**：可按类型 / 时间 / 重要性浏览，查看明文、编辑、删除、导出

### 3.4 权限模型

| 级别 | 范围 | 示例 | 默认行为 |
|------|------|------|----------|
| **read-only** | 只读文件系统 | 列目录、读文件 | 静默允许 |
| **write-project** | 项目内写入 | 修改代码、创建文件 | Ask 模式需确认，Auto 模式静默 |
| **shell** | 执行命令 | 运行测试、构建 | 沙箱内执行；网络访问可配置 |
| **git** | Git 操作 | commit、push | commit 自动，push 需确认 |
| **network** | 网络访问 | API 调用、下载依赖 | 白名单策略 |

### 3.5 错误处理与降级策略

#### 3.5.1 错误分类

```
错误严重度金字塔：

  ┌─────────────┐
  │  Fatal      │ 进程无法继续，需立即终止并上报
  │  立即终止    │ 示例：数据库损坏无法恢复、沙箱逃逸检测
  ├─────────────┤
  │  Recoverable│ 当前操作失败，但系统可继续运行
  │  操作回滚    │ 示例：LLM 超时、文件写入冲突、OOM
  ├─────────────┤
  │  Degraded   │ 成功但非最优，可降级继续
  │  降级运行    │ 示例：FAISS 不可用 → 回退全文搜索
  └─────────────┘
```

#### 3.5.2 各子系统错误处理矩阵

| 子系统 | 典型错误 | 严重度 | 处理策略 | 用户感知 |
|--------|---------|--------|---------|----------|
| **LLM 调用** | 超时（> 30s） | Recoverable | 指数退避重试 3 次（1s/2s/4s），仍失败则提示切换模型 | "模型响应超时，已重试 3 次。建议切换至其他模型或稍后重试。[切换] [重试]" |
| **LLM 调用** | 4xx 错误（Bad Request） | Recoverable | 截断上下文至上一轮，重试 1 次 | "请求格式错误，已自动调整。如持续出现请反馈。[查看详情]" |
| **LLM 调用** | 5xx / 连接拒绝 | Recoverable | 退避重试 3 次，仍失败 → 标记为 Degraded | "服务端异常 (502)，已自动重试…" |
| **LLM 调用** | Rate Limit (429) | Recoverable | 读取 Retry-After 头，等待后重试 | "请求频率限制，将在 N 秒后自动重试。[立即重试]" |
| **LLM 调用** | 输出格式不合法（JSON 解析失败） | Recoverable | 重放原始 prompt + 格式错误提示（最多 2 次） | 透明（自动修复） |
| **文件系统** | 权限拒绝（EACCES） | Recoverable | 提示用户授予权限或跳过该文件 | "无法访问 X，请检查文件权限。[授予权限] [跳过]" |
| **文件系统** | 文件被外部修改（冲突） | Recoverable | diff 应用前检查文件 hash，冲突时提示用户选择版本 | "X 文件在外部被修改，与即将应用的补丁冲突。[保留我的] [保留外部] [手动合并]" |
| **文件系统** | 磁盘空间不足 | Recoverable | 暂停写入操作，弹出清理向导 | "磁盘空间不足 (< 100MB)，部分功能暂停。" |
| **Git** | 分支冲突 / rebase 中断 | Recoverable | 暂停 AI 操作，展示当前 Git 状态，提供回滚选项 | "Git 冲突检测，请先手动解决。当前操作已暂存。[查看状态] [回滚]" |
| **Git** | 未跟踪文件覆盖风险 | Recoverable | 先 git stash 再操作 | "检测到未跟踪文件，已自动暂存 (stash)。" |
| **FAISS** | 索引损坏 | Degraded | 回退到 SQLite 全文搜索（LIKE），后台重建索引 | "语义搜索暂时不可用，已切换为关键词搜索。索引正在后台重建。" |
| **FAISS** | OOM（内存不足建索引） | Degraded | 减小索引规模（仅保留 importance > 0.5 的条目），或回退 SQLite | 透明 + 通知："部分历史记忆暂不可搜索，系统正在优化。" |
| **Sandbox** | 命令执行超时（> 120s） | Recoverable | 发送 SIGTERM，5s 后 SIGKILL，返回部分输出 | "命令执行超时 (120s)，已终止。[查看输出] [增加超时] [重试]" |
| **Sandbox** | 可疑命令检测（`rm -rf /`、`curl | sh` 等） | Recoverable | 拦截并弹确认，用户确认后仍不执行高危模式 | "命令含潜在危险操作: `rm -rf /`。已被拦截。[编辑命令] [强制执行]" |
| **IPC** | 前端-Rust 通信中断 | Fatal | 尝试重建通道，失败后提示重启 | "内部通信异常，建议保存并重启应用。[重启] [保存日志]" |
| **数据库** | SQLite 锁定（SQLITE_BUSY） | Recoverable | 等待最多 5s（WAL 模式下极少发生），超时返回错误 | 透明（自动等待） |

#### 3.5.3 降级模式总览

YourMate 定义三种全局运行模式：

| 模式 | 触发条件 | 行为 | 指示器 |
|------|---------|------|--------|
| **Full** | 所有子系统正常 | 全部功能可用 | 无 |
| **Degraded** | FAISS / Linter / 远程 LLM 不可用 | 禁用受影响功能，核心聊天和文件编辑仍可用 | 状态栏黄色图标 + 降级项灰显 |
| **Offline** | 网络断开 + 无本地模型 | 仅浏览文件树和会话历史，禁止执行和 AI 调用 | 状态栏红色图标 + 所有 AI 功能禁用 |

降级检查流程（每个操作前）：

```
操作请求
  │
  ├─→ 检查依赖子系统状态
  │     ├─ Full → 执行
  │     ├─ Degraded + 操作兼容 → 执行（记录告警日志）
  │     └─ Degraded + 操作不兼容 → 返回错误 + 降级提示
  │     └─ Offline → 返回错误（"此功能需要网络连接或本地模型"）
```

降级恢复：系统每 30s 探测不可用服务（轻量 health check），一旦恢复自动切回 Full 模式并推送通知。

#### 3.5.4 Crash Recovery 流程

```
应用崩溃
  │
  ▼
下次启动 → 检查 recovery/ 目录
  │
  ├─ 存在 recovery 文件 → 弹出恢复对话框:
  │   ┌──────────────────────────────────┐
  │   │ ⚠ 检测到上次非正常退出             │
  │   │                                  │
  │   │ 最后会话: "修复 login bug"        │
  │   │ 未保存消息: 3 条                  │
  │   │ 未应用变更: 2 个文件              │
  │   │                                  │
  │   │ [恢复会话]  [丢弃]  [查看详情]     │
  │   └──────────────────────────────────┘
  │
  └─ 无 recovery 文件 → 正常启动
```

Recovery 文件内容（每隔 30s 自动写入）：

```json
{
  "session_id": "uuid",
  "project_id": "uuid",
  "last_message_at": "2026-06-27T10:30:00Z",
  "pending_changes": [
    { "file": "src/main.rs", "diff_hash": "sha256..." }
  ],
  "draft_input": "用户未发送的输入内容"
}
```

#### 3.5.5 用户输入校验规则

| 校验项 | 规则 | 拒绝时提示 |
|--------|------|----------|
| 消息长度 | ≤ 65,536 字符 | "消息过长，请分段发送或使用文件上传" |
| 粘贴图片 | ≤ 10MB，格式: PNG/JPEG/WebP | "图片过大 (最大 10MB) / 不支持的格式" |
| 粘贴文件 | ≤ 20MB 文本文件 | "文件过大 (最大 20MB)" |
| `/` 命令 | 仅允许注册的命令列表 | "未知命令: /xxx。输入 / 查看可用命令" |
| URL | 禁止 `file://` 协议，仅允许 `https://` | "仅支持 HTTPS 链接" |

---

### 4.1 技术选型结论

经过对比分析，选定以下技术栈：

| 层 | 技术 | 选型理由 |
|----|------|----------|
| **桌面壳** | Tauri v2 | 包体 < 5MB，内存占用低，Rust 原生系统调用 |
| **核心服务** | Rust | 与 Tauri 同进程零开销 IPC；无 GC，适合长期驻留；天然并发安全 |
| **前端 UI** | React 18 + TypeScript | 生态成熟，社区资源丰富，开发效率高 |
| **编辑器** | Monaco Editor | VS Code 同款内核，diff 渲染原生支持 |
| **状态管理** | Zustand | 轻量（< 1KB），API 简洁，TypeScript 友好 |
| **UI 样式** | Tailwind CSS | 原子化，主题切换方便，构建体积小 |
| **数据库** | SQLite (rusqlite) | 零配置，嵌入式，与 Rust 生态集成好 |
| **向量索引** | FAISS C API (faiss-sys) | Rust 直接绑定，无需子进程或 Python 桥接 |
| **IPC** | Tauri invoke + event | 前端调 Rust 用 invoke，Rust 推前端用 event |
| **Git 操作** | git2 (libgit2 Rust binding) | 纯 Rust 绑定，无需外部 Git CLI |
| **命令沙箱** | 自定义 sandbox crate | 基于 OS 级进程隔离 + 目录白名单 |
| **加密** | AES-256-GCM (ring crate) | 纯 Rust 实现，无外部依赖 |
| **测试框架** | Rust: cargo test / 前端: Vitest + Playwright | 标准方案 |

#### 备选方案对比

| 对比维度 | 选定方案 (Tauri + Rust) | 备选方案 (Electron + Node.js) |
|----------|------------------------|-------------------------------|
| 安装包体积 | ~8MB | ~150MB |
| 内存占用（空闲） | ~80MB | ~200MB |
| 文件遍历（10 万文件） | ~0.5s | ~3s |
| FAISS 集成 | 原生绑定 | 需 native addon 或子进程 Python |
| 开发效率 | Rust 学习曲线 | JS 全栈，上手快 |
| 社区生态 | 快速增长 | 极其成熟 |

> **结论**：当团队有 1 名以上 Rust 经验成员时选 Tauri；否则 MVP 阶段可先 Electron + Node.js，将性能热点（FAISS、大文件索引）用 Rust native addon 补充，后续逐步迁移。

### 4.2 前端技术细节

```
React 18 + TypeScript (strict mode)
├── Monaco Editor (代码编辑 + diff 渲染)
├── Zustand (全局状态)
├── Tailwind CSS (样式)
├── @tauri-apps/api (IPC 通信)
├── react-router (页面路由)
├── Vitest (单元测试)
└── Playwright (E2E 测试)
```

### 4.3 数据库 Schema 设计

#### 4.3.1 项目与配置

```sql
CREATE TABLE projects (
    id          TEXT PRIMARY KEY,          -- UUID v4
    name        TEXT NOT NULL,
    local_path  TEXT NOT NULL UNIQUE,      -- 本地绝对路径
    language    TEXT,                      -- ts, py, rust, go...
    framework   TEXT,                      -- react, django, axum...
    git_remote  TEXT,                      -- origin URL
    created_at  TEXT NOT NULL,             -- ISO 8601
    opened_at   TEXT,                      -- 上次打开时间
    settings    TEXT NOT NULL DEFAULT '{}' -- JSON: 项目级配置
);

CREATE TABLE settings (
    scope       TEXT NOT NULL CHECK(scope IN ('global','project')),
    project_id  TEXT REFERENCES projects(id) ON DELETE CASCADE,
    key         TEXT NOT NULL,
    value       TEXT NOT NULL,             -- JSON value
    UNIQUE(scope, project_id, key)
);
```

#### 4.3.2 会话与消息

```sql
CREATE TABLE sessions (
    id          TEXT PRIMARY KEY,
    project_id  TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    title       TEXT,                      -- 自动生成或用户命名
    mode        TEXT NOT NULL DEFAULT 'ask'
                CHECK(mode IN ('ask','plan','edit','auto')),
    status      TEXT NOT NULL DEFAULT 'active'
                CHECK(status IN ('active','archived','merged')),
    parent_id   TEXT REFERENCES sessions(id), -- 分支会话
    token_count INTEGER DEFAULT 0,        -- 累计 token 消耗
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

CREATE TABLE messages (
    id          TEXT PRIMARY KEY,
    session_id  TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
    role        TEXT NOT NULL CHECK(role IN ('user','assistant','system','tool')),
    content     TEXT NOT NULL,             -- markdown
    tool_calls  TEXT,                      -- JSON: AI 调用的工具记录 [{tool, args, result}]
    artifacts   TEXT,                      -- JSON: #{diff_ids: [...], file_refs: [...]}
    token_usage TEXT,                      -- JSON: {prompt: N, completion: N}
    created_at  TEXT NOT NULL
);

CREATE INDEX idx_messages_session ON messages(session_id, created_at);
```

#### 4.3.3 变更追踪

```sql
CREATE TABLE changes (
    id          TEXT PRIMARY KEY,
    session_id  TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
    message_id  TEXT REFERENCES messages(id),
    file_path   TEXT NOT NULL,             -- 相对项目根路径
    diff_text   TEXT NOT NULL,             -- unified diff
    status      TEXT NOT NULL DEFAULT 'pending'
                CHECK(status IN ('pending','applied','rejected','reverted')),
    snapshot_id TEXT,                      -- 关联的 git 快照，用于回滚
    created_at  TEXT NOT NULL,
    applied_at  TEXT
);

CREATE INDEX idx_changes_session ON changes(session_id);
CREATE INDEX idx_changes_file ON changes(file_path);
```

#### 4.3.4 长期记忆

```sql
CREATE TABLE memories (
    id              TEXT PRIMARY KEY,
    project_id      TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    type            TEXT NOT NULL CHECK(type IN ('fact','interaction','behavior','plan')),

    -- 内容
    content         TEXT NOT NULL,         -- 自然语言原文
    summary         TEXT,                  -- 压缩后的摘要（高版本覆盖低版本）
    embedding       BLOB,                  -- float32 向量（FAISS 主存，SQLite 存副本）

    -- 元数据
    source_session_id TEXT REFERENCES sessions(id),
    importance      REAL NOT NULL DEFAULT 0.5, -- 0-1 权重
    access_count    INTEGER DEFAULT 0,
    last_accessed_at TEXT,

    -- 压缩与生命周期
    version         INTEGER DEFAULT 1,     -- 压缩版本号，每次压缩 +1
    compressed_from TEXT,                  -- JSON: 被合并的源记忆 ID 列表
    expires_at      TEXT,                  -- NULL = 永不过期

    -- 隐私
    encrypted       INTEGER NOT NULL DEFAULT 0, -- 是否加密存储

    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);

-- 记忆关联（知识图谱边）
CREATE TABLE memory_links (
    source_id   TEXT NOT NULL REFERENCES memories(id) ON DELETE CASCADE,
    target_id   TEXT NOT NULL REFERENCES memories(id) ON DELETE CASCADE,
    relation    TEXT NOT NULL,             -- related_to | depends_on | contradicts | extends
    weight      REAL DEFAULT 1.0,
    PRIMARY KEY (source_id, target_id, relation)
);

-- 向量索引元数据
CREATE TABLE vector_index_meta (
    id              TEXT PRIMARY KEY,
    project_id      TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    dimension       INTEGER NOT NULL,      -- 向量维度（由 embedding 模型决定）
    index_path      TEXT NOT NULL,         -- FAISS 索引文件路径
    entry_count     INTEGER DEFAULT 0,
    last_rebuilt_at TEXT,
    UNIQUE(project_id)
);

CREATE INDEX idx_memories_project_type ON memories(project_id, type);
CREATE INDEX idx_memories_importance ON memories(project_id, importance DESC);
CREATE INDEX idx_memories_accessed ON memories(project_id, last_accessed_at DESC);
```

#### 4.3.5 Skill 插件

```sql
CREATE TABLE skills (
    id          TEXT PRIMARY KEY,
    name        TEXT NOT NULL UNIQUE,
    version     TEXT NOT NULL,
    source      TEXT NOT NULL,             -- builtin | registry_url | local_path
    entry_point TEXT NOT NULL,             -- 入口文件相对路径
    permissions TEXT NOT NULL DEFAULT '[]', -- JSON: 所需权限列表
    enabled     INTEGER NOT NULL DEFAULT 1,
    installed_at TEXT NOT NULL
);
```

#### 4.3.6 审计日志

```sql
CREATE TABLE audit_log (
    id          TEXT PRIMARY KEY,
    session_id  TEXT NOT NULL REFERENCES sessions(id),
    message_id  TEXT REFERENCES messages(id),
    command     TEXT NOT NULL,             -- 执行的命令
    cwd         TEXT NOT NULL,             -- 执行目录
    exit_code   INTEGER,
    stdout      TEXT,
    stderr      TEXT,
    duration_ms INTEGER,
    sandboxed   INTEGER NOT NULL DEFAULT 1,
    created_at  TEXT NOT NULL
);

CREATE INDEX idx_audit_session ON audit_log(session_id, created_at);
```

#### 4.3.7 实体关系总览

```
Project 1──N Session 1──N Message
   │                    │
   │                    └──1──N Change
   │
   ├──1──N Memory ──N──N Memory (via memory_links)
   │
   ├──1──1 VectorIndexMeta
   │
   └──1──N AuditLog
```

---

## 5 开发步骤

### 阶段 0：技术 POC（0–2 周）

**目标**：验证核心选型可行，锁定技术栈。POC 只验证风险最高的假设，不写产品代码。

#### POC-1: Tauri v2 + React IPC 通信

| 项 | 内容 |
|----|------|
| **验证目标** | Tauri invoke 通信延迟是否满足 UI 交互要求 |
| **方法** | 搭建 Tauri v2 + React 最小骨架；构造 3 种 IPC 场景并测量 |
| **场景 A** | 调用 Rust 端读取项目目录树（5000 文件），测量 invoke 往返延迟 |
| **场景 B** | Rust 端流式推送 LLM token 到前端（event 机制），测量单 token 送达延迟 |
| **场景 C** | 前端连续调用 100 次 Rust 函数，测量总耗时与单次平均 |
| **通过标准** | 场景 A < 50ms；场景 B 单 token < 10ms；场景 C 平均 < 5ms |
| **失败回退** | 若 IPC 延迟超标 → 评估是否改用共享内存方案或降级为 Electron + contextBridge |
| **预期产出** | `poc/tauri-ipc/`：骨架项目 + 性能测试脚本 + 测量报告 |

#### POC-2: FAISS 向量索引集成

| 项 | 内容 |
|----|------|
| **验证目标** | faiss-sys 能否在三大平台编译通过，以及索引/查询性能是否达标 |
| **方法** | 使用 text-embeddings-infer (Rust) 生成 768 维向量，FAISS IndexFlatIP 索引 |
| **数据集** | 构造 1 万 / 5 万 / 10 万条模拟 memory embedding |
| **测量指标** | 索引构建时间、索引文件大小、Top-K 查询延迟 (K=5, 10, 20)、召回率 vs 暴力搜索 |
| **通过标准** | 1 万条索引 < 5s，查询 < 10ms (K=10)；10 万条查询 < 50ms；召回率 > 99% |
| **失败回退** | usearch (纯 Rust，无 C++ 编译负担) 或 pgvector (通过 PostgreSQL) |
| **预期产出** | `poc/faiss-bench/`：基准测试代码 + 三平台 CI 结果 + 对比报告 |

#### POC-3: Monaco Editor 集成

| 项 | 内容 |
|----|------|
| **验证目标** | Monaco 在 Tauri webview 中的加载速度和 diff 渲染性能 |
| **方法** | React 组件中加载 Monaco，测试语法高亮和 side-by-side diff 两种模式 |
| **场景 A** | 打开 1000 行 TypeScript 文件，测量从 onOpen 到可交互的时间 |
| **场景 B** | 渲染 5000 行 unified diff，测量渲染时间和滚动帧率 |
| **场景 C** | 连续快速切换 10 个文件，测量内存增长 |
| **通过标准** | A < 500ms；B 渲染 < 1s，滚动 30fps+；C 内存增长 < 50MB |
| **失败回退** | 若 diff 性能不足 → 大 diff 自动折叠，仅展示变更区域上下文；备选 CodeMirror 6 |
| **预期产出** | `poc/monaco-test/`：测试页面 + 性能数据 + 内存快照 |

#### POC-4: 大仓库文件树性能

| 项 | 内容 |
|----|------|
| **验证目标** | 虚拟滚动方案能否应对 10 万+ 文件的项目 |
| **方法** | 用 Rust 端实现文件树扫描（ignore crate 或 walkdir），前端用 @tanstack/react-virtual 渲染 |
| **测试数据** | 用脚本生成模拟仓库：10 万文件 / 1000 目录 / 深度 10 层 |
| **场景 A** | 冷启动首次扫描全量文件并发送到前端的时间 |
| **场景 B** | 展开深度嵌套目录 (深度 8) 的延迟 |
| **场景 C** | 文件监听 (notify crate) 下新增/删除文件的更新延迟 |
| **通过标准** | A < 2s；B < 100ms；C 更新延迟 < 500ms |
| **失败回退** | 按需加载子目录（非预加载全量）；排除列表可配置 |
| **预期产出** | `poc/filetree/`：骨架 + 测试数据生成脚本 + 性能数据 |

#### POC-5: git2 (libgit2) 基本操作

| 项 | 内容 |
|----|------|
| **验证目标** | git2 crate 能否替代 Git CLI 完成核心操作，以及对大仓库的性能 |
| **方法** | 在真实项目（Linux 内核源码或 Chromium 任意子目录）上执行操作 |
| **场景 A** | `status` — 获取工作区状态（文件是否变更） |
| **场景 B** | `diff` — 生成指定文件的 unified diff |
| **场景 C** | `commit` — 创建提交，包含 author/committer 信息 |
| **场景 D** | `branch` — 列出所有分支、创建新分支 |
| **通过标准** | A + B + D 各 < 500ms；C < 2s (含树写入) |
| **失败回退** | 回退为 Git CLI 封装（Command::new("git")），通过 --porcelain 标志解析输出 |
| **预期产出** | `poc/git2-test/`：测试代码 + 与大仓库的性能对比 |

#### POC 汇总与决策门

```
Week 1: 并行启动 POC-1 ~ POC-5
Week 2 前 3 天: 收尾、补测、写报告
Week 2 后 2 天: POC 评审会议

评审议程:
  1. 各 POC 通过/失败状态
  2. 失败项的回退方案可行性评估
  3. 最终技术栈锁定决策 (Tauri+Rust vs Electron+Node.js 降级)
  4. Phase 1 排期是否需调整
```

**决策规则**：
- 5/5 通过 → 锁定 Tauri + Rust，Phase 1 按原排期
- 3~4/5 通过 + 回退可行 → 锁定但有条件，Phase 1 增加 1-2 周缓冲
- < 3/5 通过 + 回退不可行 → 降级为 Electron + Node.js，重新评估排期

**POC 产出物清单**：

```
poc/
├── README.md              # POC 总览 + 决策结论
├── tauri-ipc/
│   ├── src-tauri/         # Tauri Rust 端
│   ├── src/               # React 前端
│   └── REPORT.md          # IPC 性能测试报告
├── faiss-bench/
│   ├── src/
│   ├── data/              # 模拟 embedding 数据生成脚本
│   └── REPORT.md          # FAISS 基准测试报告
├── monaco-test/
│   ├── src/
│   └── REPORT.md          # Monaco 性能报告
├── filetree/
│   ├── src/
│   ├── gen-test-repo.sh   # 测试仓库生成脚本
│   └── REPORT.md          # 文件树性能报告
└── git2-test/
    ├── src/
    └── REPORT.md          # git2 功能验证报告
```

### 阶段 1：基础 UI 与项目打开（2–8 周）

- 主页 UI：项目卡片、历史记录、搜索栏
- 项目打开流程：选择本地目录 → 文件树展示（虚拟滚动）
- 聊天 UI：消息流（虚拟列表）、输入框、Markdown 渲染
- 会话管理：创建、切换、重命名、删除、搜索
- 基础模型调用：Bosch Assistant 模式回答问题（流式输出）
- 设置页面骨架

**验收标准**：
- 完成 Loading / Empty / Error 三种状态覆盖
- 10 万文件项目文件树操作流畅（展开/折叠 < 100ms）
- 流式输出首 token < 1s

### 阶段 2：Git 与变更视图（8–12 周）

- GitManager：status、diff、commit、branch 列表、log
- 本次变更视图：session 维度的文件变更列表
- 可视化 diff 卡片：Monaco diff 渲染，部分采纳 / 回滚
- CodeEditor：补丁 dry-run → apply 基础流程
- Git 快照：变更前后自动记录，支持回滚

**验收标准**：
- diff 渲染支持 5000 行以内文件无明显延迟
- 回滚后文件内容与快照一致（SHA256 校验）

### 阶段 3：长期记忆与检索（12–16 周）

- SQLite Schema 初始化与 migration 机制
- FAISS 向量索引创建、增量更新、定时重建
- Retriever：语义相似度 + 时间衰减 + 重要性加权融合检索
- MemoryCompressor：定期聚合相似条目 → 生成摘要 → 更新版本链
- 记忆审查界面：按类型/时间/重要性浏览、搜索、编辑、删除、导出

**验收标准**：
- 检索延迟 < 50ms（1 万条记忆规模）
- 压缩后语义信息保留率 > 80%（人工抽样评估）
- 加密记忆在数据库中以密文存储，审查界面需输入密钥后查看

### 阶段 4：AI Loop 初版（16–20 周）

- Auto 模式：理解 → 计划 → dry-run → apply → 验证 完整流程
- SandboxExecutor：命令执行沙箱（OS 级进程隔离 + 项目目录白名单）
- TestRunner：自动发现测试 → 执行 → 解析结果 → 反馈给 AI
- LinterAnalyzer：集成常见 linter（eslint、ruff、clippy）→ 解析输出
- 审计日志：命令执行全量记录

**验收标准**：
- Auto 模式完成一次"修改 → 测试 → lint"闭环，无需人工干预
- 沙箱内命令无法访问项目目录外的文件（自动化测试验证）
- 审计日志可回溯任意一次命令执行的完整信息

### 阶段 5：高级模式与安全（20–26 周）

- Ask before edits 完整流程：每个变更需用户确认后才 apply
- Plan 模式：PlanGenerator 生成结构化执行计划，用户可编辑后提交
- SafetyGuard：基于权限模型的操作拦截（见 3.4 节）
- PR 草稿生成：自动生成 PR 标题和描述
- 加密备份与导入导出：项目记忆 + 会话 + 设置的完整备份
- Onboarding 流程：首次使用引导

**验收标准**：
- Ask 模式下任何代码写入操作均弹出确认卡片
- Plan 文档导出为 Markdown，可被其他工具读取
- 备份文件可跨设备导入还原

### 阶段 6：Beta 测试与稳定（26–30 周）

- 内部 Beta 测试（5-10 人）
- 性能优化（启动时间、内存占用达标）
- 文档编写：用户手册、开发者文档、Skill 开发指南
- 自动更新机制
- 崩溃报告收集

**验收标准**：
- 所有非功能性指标达标（见 1.3 节）
- 用户文档覆盖所有主要功能
- Beta 期间 P0/P1 bug 清零

### 风险与依赖

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| faiss-sys 编译/性能不达预期 | 向量检索方案受阻 | Phase 0 优先验证；备选：usearch (纯 Rust) |
| Tauri v2 移动端需求不匹配 | 若后期需要移动端 | Tauri v2 已支持 Android/iOS，风险较低 |
| LLM API 变更或不可用 | 核心功能依赖 | 适配层抽象接口，支持多后端切换 |
| 单人/小团队进度压力 | 排期延迟 | 每阶段有明确的 MVP 裁剪范围 |
| 大项目文件树性能 | 用户体验差 | 虚拟滚动 + 增量加载 + 黑名单过滤（node_modules/.git） |

---

## 6 测试策略与 CI/CD

### 6.1 测试金字塔

```
          ╱  E2E  ╲         5% — 关键用户路径
         ╱  (Playwright) ╲
        ╱───────────────────╲
       ╱   集成测试           ╲   20% — 子系统边界
      ╱  (Rust + Playwright)   ╲
     ╱──────────────────────────╲
    ╱      单元测试               ╲  75% — 核心逻辑覆盖
   ╱  (cargo test + Vitest)        ╲
  ╱──────────────────────────────────╲
```

### 6.2 各层测试策略

#### 6.2.1 单元测试

| 层 | 框架 | 覆盖目标 | 重点 |
|----|------|---------|------|
| **Rust Core** | `cargo test` + `proptest` | 行覆盖 ≥ 70% | AI Loop 编排逻辑、diff 生成/应用、记忆压缩、输入校验、加密/解密 |
| **React UI** | Vitest + React Testing Library | 组件覆盖 ≥ 60% | 状态管理逻辑、Markdown 渲染、虚拟列表分页、表单校验 |
| **共享类型** | 编译期检查 | 类型覆盖 100% | Tauri command 的输入输出类型、IPC 事件类型 |

Rust 侧利用 property-based testing 覆盖边界：

```rust
// 示例：diff 应用的回滚正确性
proptest! {
    fn diff_apply_revert_is_idempotent(original: String, patch: String) {
        // 应用补丁 → 反转 → 应与原始一致
    }
}
```

前端组件测试聚焦逻辑而非渲染细节：

```typescript
// 示例：消息列表虚拟滚动逻辑
describe('MessageList', () => {
  it('only renders visible messages + overscan', () => {
    const { container } = render(<MessageList messages={generate(10000)} />);
    expect(container.querySelectorAll('.message').length).toBeLessThan(50);
  });
});
```

#### 6.2.2 集成测试

**Rust 侧（cargo test --test integration）**：

| 测试套件 | 内容 | 数据 |
|----------|------|------|
| `db_tests` | SQLite schema migration、CRUD、WAL recovery、并发读写 | 临时 SQLite 文件 |
| `git_tests` | git2 操作在临时仓库中的行为：status、diff、commit、stash、conflict | 临时 Git 仓库 |
| `faiss_tests` | 索引创建、增删查、重建、损坏恢复 | 临时 FAISS 索引 |
| `sandbox_tests` | 命令执行、超时、路径限制、网络隔离 | 临时目录 |
| `memory_tests` | 记忆 CRUD、压缩、检索精度、加密往返 | 临时数据库 + FAISS |
| `ai_loop_tests` | Mock LLM 下的理解→计划→执行→验证流程 | Mock LLM 后端 |

**前端侧（Playwright Component Testing）**：

| 测试套件 | 内容 |
|----------|------|
| `ipc_tests` | 前端通过 Tauri invoke mock 调用 Rust 命令，验证数据流 |
| `diff_card_tests` | diff 卡片的接受/拒绝/部分采纳交互 |
| `file_tree_tests` | 虚拟滚动的展开、搜索、右键菜单 |

#### 6.2.3 E2E 测试

使用 Playwright，覆盖以下关键用户路径：

```
E2E-1: 新用户引导
  打开应用 → Onboarding 向导 → 选择本地项目 → 配置模型 → 进入聊天
  → 输入 "这个项目是做什么的？" → 验证收到流式回复

E2E-2: Edit 模式闭环
  进入 Edit 模式 → 输入 "把 config.ts 的端口改成 8080"
  → 验证 dry-run diff 卡片出现 → 点击 Apply → 验证文件实际修改
  → 验证右侧变更视图显示该变更

E2E-3: Git 提交流程
  在上述变更后 → 切换到 Git 视图 → 验证文件在 unstaged 列表
  → Stage → 输入 commit message → Commit → 验证 commit 出现在 log 中

E2E-4: 会话管理
  创建新会话 → 发送消息 → 切换到另一个会话 → 切回 → 验证历史消息完整

E2E-5: 设置页
  进入设置 → 修改模型参数 → 保存 → 重启 → 验证参数持久化

E2E-6: 错误降级
  断开网络 → 发送消息 → 验证错误提示 → 重连 → 验证恢复正常
```

### 6.3 测试基础设施

```
测试环境矩阵：
  ├── CI (每次 push / PR):
  │   ├── Ubuntu 24.04 (x86_64)    ← 主力 CI
  │   ├── Windows 11 (x86_64)      ← GitHub Actions Windows runner
  │   └── macOS 15 (ARM64)         ← GitHub Actions M1 runner
  │
  └── 本地 (每次 commit 前):
      └── cargo test + vitest (pre-commit hook)
```

### 6.4 CI/CD 流水线

```
┌─────────────────────────────────────────────────────────────┐
│  CI Pipeline (feature branch → PR)                           │
│                                                             │
│  Push ──→ ┌──────────┐ ┌──────────┐ ┌──────────────┐       │
│           │ Lint     │ │ Type     │ │ Unit Tests   │       │
│           │ clippy   │ │ Check    │ │ cargo test   │       │
│           │ eslint   │ │ tsc      │ │ vitest       │       │
│           │ (2 min)  │ │ (1 min)  │ │ (3 min)      │       │
│           └──────────┘ └──────────┘ └──────┬───────┘       │
│                                            │               │
│              ┌─────────────────────────────┘               │
│              ▼                                             │
│     ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│     │ Integration  │  │ Security     │  │ Build        │   │
│     │ Tests        │  │ Audit        │  │ (各平台)      │   │
│     │ (5 min)      │  │ cargo audit  │  │ (8 min)      │   │
│     │              │  │ npm audit    │  │              │   │
│     └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
│            │                 │                 │           │
│            └─────────────────┴─────────────────┘           │
│                              │                             │
│                              ▼                             │
│                     ┌──────────────┐                       │
│                     │ E2E Tests    │                       │
│                     │ Playwright   │                       │
│                     │ (10 min)     │                       │
│                     └──────┬───────┘                       │
│                            │                               │
│              ┌─────────────┘                               │
│              ▼                                             │
│     ┌────────────────┐                                     │
│     │ PR Gate Check  │                                     │
│     │ ✅ All green   │ → Merge allowed                     │
│     │ ❌ Any red     │ → Block merge                       │
│     └────────────────┘                                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  CD Pipeline (main → release)                                │
│                                                             │
│  Merge to main ──→ CI full suite ──→ Version Bump           │
│                                      (changeset)            │
│                                      │                      │
│                              ┌───────┴───────┐              │
│                              ▼               ▼              │
│                       ┌──────────┐   ┌──────────┐          │
│                       │ Build    │   │ Changelog│          │
│                       │ Win/Mac/ │   │ Generate │          │
│                       │ Linux    │   │          │          │
│                       └────┬─────┘   └────┬─────┘          │
│                            │              │                 │
│                            ▼              │                 │
│                       ┌──────────┐        │                 │
│                       │ Sign     │        │                 │
│                       │ (ed25519)│        │                 │
│                       └────┬─────┘        │                 │
│                            │              │                 │
│                            ▼              ▼                 │
│                       ┌──────────────────────┐              │
│                       │ GitHub Release Draft │              │
│                       │ + 更新服务器上传       │              │
│                       └──────────────────────┘              │
└─────────────────────────────────────────────────────────────┘
```

### 6.5 各阶段测试要求

| 阶段 | 单元测试 | 集成测试 | E2E | CI 配置 |
|------|---------|---------|-----|---------|
| **Phase 0** | POC 基准测试 | 无 | 无 | 仅 lint + build |
| **Phase 1** | UI 组件 + Rust Core | 数据库 migration | E2E-1 (Onboarding) | 完整 CI (Ubuntu) |
| **Phase 2** | diff/git 逻辑 | git_tests | E2E-2, E2E-3 | + Windows |
| **Phase 3** | 记忆/检索逻辑 | memory_tests, faiss_tests | E2E-4 | 不变 |
| **Phase 4** | sandbox/runner 逻辑 | sandbox_tests, ai_loop_tests | E2E-5 | + macOS |
| **Phase 5** | 权限/安全逻辑 | 全量 | E2E-6 | 三平台全量 |
| **Phase 6** | 恢复/备份逻辑 | 全量 | 全量 | 三平台全量 |

### 6.6 质量门禁 (Quality Gates)

每个 PR 合并前必须通过：

| 门禁 | 条件 | 阻断级别 |
|------|------|----------|
| **Lint** | `cargo clippy -- -D warnings` + `eslint --max-warnings 0` | 阻断 |
| **Type Check** | `tsc --noEmit` 无错误 | 阻断 |
| **单元测试** | 全部通过，覆盖率不低于上次 commit | 阻断 (覆盖率下降时警告) |
| **集成测试** | 全部通过 | 阻断 |
| **安全审计** | `cargo audit` + `npm audit` 无 Critical/High | 阻断 |
| **构建** | 三平台构建成功 | 阻断 |
| **E2E** | 全部通过 | 阻断 (Phase 3 起) |
| **Bundle 体积** | 不超过上次 release 的 +20% | 警告 |

---

## 7 Skill 插件接口规范

### 7.1 设计目标

- Skill 是一个独立的 TypeScript/JavaScript 模块，在**沙箱环境中运行**
- 用户从内置目录或注册表安装，安装前需显式授权权限
- AI 可调用 Skill 作为工具使用；用户也可通过 `/skill-name` 直接调用
- 每个 Skill 声明其所需权限、输入 Schema、输出 Schema

### 7.2 插件清单 (Skill Manifest)

每个 Skill 根目录需包含 `YourMate.skill.json`（或 `YourMate.skill.yaml`）：

```jsonc
{
  "$schema": "https://YourMate.dev/skill-schema/v1",
  "name": "generate-changelog",               // 唯一标识 (kebab-case)
  "display_name": "Changelog Generator",
  "version": "1.2.0",
  "description": "基于 Git 提交历史自动生成 changelog",
  "author": {
    "name": "Example Author",
    "email": "author@example.com"
  },
  "license": "MIT",
  "homepage": "https://github.com/...",

  // 入口文件（相对于 Skill 根目录）
  "entry": "index.ts",

  // 运行时
  "runtime": {
    "type": "deno",                          // deno | node | wasm
    "version": ">=1.40.0"
  },

  // 权限声明（运行前弹窗让用户确认）
  "permissions": {
    "filesystem": {
      "scope": "project",                    // project | skill_dir | none
      "access": ["read"]                     // read | write | read_write
    },
    "shell": {
      "allowed": false                       // 此 Skill 是否可执行命令
    },
    "network": {
      "allowed": false
    },
    "git": {
      "scope": "project",
      "access": ["read"]                     // read | write
    }
  },

  // AI 工具声明（Skill 暴露给 AI Agent 调用的工具）
  "tools": [
    {
      "name": "generate_changelog",
      "description": "根据 Git 提交历史生成 Markdown 格式的 changelog",
      "parameters": {
        "type": "object",
        "properties": {
          "from_tag": {
            "type": "string",
            "description": "起始 tag，如 v1.0.0"
          },
          "to_tag": {
            "type": "string",
            "description": "结束 tag，默认 HEAD"
          },
          "style": {
            "type": "string",
            "enum": ["conventional", "simple"],
            "default": "conventional"
          }
        },
        "required": ["from_tag"]
      }
    }
  ],

  // 用户命令声明（/ 命令）
  "commands": [
    {
      "name": "changelog",
      "description": "快速生成 changelog",
      "arguments": [
        {
          "name": "from",
          "description": "起始版本 tag",
          "required": true
        }
      ]
    }
  ],

  // 依赖
  "dependencies": {
    "@std/semver": ">=1.0.0"
  },

  // 兼容性
  "YourMate": {
    "min_version": "0.1.0"
  }
}
```

### 7.3 Skill 生命周期

```
Install → Configure → Enable → [Run | AI Call | Command] → Update → Disable → Uninstall
  │         │          │                                     │
  │         │          │                                     │
  │         │     ┌────▼─────┐                               │
  │         │     │ 运行时沙箱 │                               │
  │         │     │ (Deno)   │                               │
  │         │     └──────────┘                               │
  │         │                                                │
  │    ┌────▼─────┐                                          │
  │    │ 用户确认   │ ← 展示权限清单                           │
  │    │ 权限授权   │                                         │
  │    └──────────┘                                          │
  │                                                          │
  └── 下载/复制 → 校验 manifest → 写入 skills/ 目录 ─────────┘
```

### 7.4 Skill API（Rust trait 定义）

```rust
/// Skill 管理 trait
pub trait SkillManager {
    /// 安装 Skill（从注册表 URL 或本地路径）
    fn install(&self, source: SkillSource) -> Result<Skill, InstallError>;

    /// 卸载 Skill（可选是否删除数据）
    fn uninstall(&self, skill_id: &str, remove_data: bool) -> Result<(), UninstallError>;

    /// 列出所有已安装 Skill
    fn list(&self) -> Vec<SkillInfo>;

    /// 启用/禁用 Skill
    fn set_enabled(&self, skill_id: &str, enabled: bool) -> Result<(), Error>;

    /// 更新 Skill 到最新版本
    fn update(&self, skill_id: &str) -> Result<Skill, UpdateError>;
}

/// AI 工具调用 trait——Skill 暴露给 AI 的函数
pub trait SkillTool {
    /// 工具名称（AI 看到的名字）
    fn name(&self) -> &str;

    /// 工具描述（AI 用来判断何时调用）
    fn description(&self) -> &str;

    /// 参数 JSON Schema
    fn parameters(&self) -> serde_json::Value;

    /// 执行工具调用
    /// - `sandbox`: 受限的沙箱上下文（文件 IO、网络等通过此接口）
    /// - `args`: AI 传入的 JSON 参数
    fn execute(
        &self,
        sandbox: &dyn SandboxContext,
        args: serde_json::Value,
    ) -> Result<ToolOutput, ToolError>;
}

/// 沙箱上下文——Skill 只能通过此接口访问系统资源
pub trait SandboxContext {
    /// 读取项目文件（受权限 scope 限制）
    fn read_file(&self, path: &RelativePath) -> Result<String, SandboxError>;

    /// 写入项目文件（需 write 权限 + 在 scope 内）
    fn write_file(&self, path: &RelativePath, content: &str) -> Result<(), SandboxError>;

    /// 执行命令（需 shell 权限）
    fn exec(&self, cmd: &str, args: &[&str]) -> Result<ExecOutput, SandboxError>;

    /// HTTP 请求（需 network 权限 + 域名白名单匹配）
    fn fetch(&self, req: HttpRequest) -> Result<HttpResponse, SandboxError>;

    /// Git 操作（需 git 权限）
    fn git(&self, op: GitOp) -> Result<GitResult, SandboxError>;

    /// 记录日志
    fn log(&self, level: LogLevel, message: &str);
}
```

### 7.5 安全模型

```
┌──────────────────────────────────────┐
│  Skill 沙箱 (Deno Isolate)           │
│                                      │
│  Skill 代码                           │
│    │                                 │
│    ├─→ read_file("src/main.rs")  ──→ fs gate (检查 filesystem scope)
│    │                                 │  ✓ 在项目目录内 + read 权限
│    │                                 │
│    ├─→ write_file("/etc/hosts") ──→ fs gate
│    │                                 │  ✗ 不在 scope 内 → 拒绝 + 告警
│    │                                 │
│    ├─→ fetch("https://evil.com")──→ net gate
│    │                                 │  ✗ 不在白名单 → 拒绝
│    │                                 │
│    └─→ exec("rm -rf /")        ──→ shell gate
│                                      │  ✗ shell 权限为 false → 拒绝
│                                      │
│  资源限制:                             │
│  - CPU 时间: 30s/session              │
│  - 内存: 256MB                        │
│  - 文件描述符: 100                     │
└──────────────────────────────────────┘
```

### 7.6 Skill 注册表

**MVP 阶段**：不实现注册表服务器，Skill 通过以下方式安装：

1. **内置 Skill**：随应用打包（`skills/builtin/`）
2. **本地路径**：用户选择本地目录安装
3. **Git URL**：`YourMate install https://github.com/user/skill-repo`

**长期规划**：

```
skill-registry 服务 (独立项目):
  - skill 上传 / 审核 / 版本管理
  - 社区评分和评论
  - 安全扫描（manifest 校验 + 代码静态分析）
  - 客户端: YourMate skill search "xxx"
```

### 7.7 内置 Skill 列表 (MVP)

| Skill | 功能 | 权限 | 命令 |
|-------|------|------|------|
| `run-tests` | 自动发现并运行项目测试 | shell (白名单命令) | `/test` |
| `format-code` | 运行格式化工具 (prettier/rustfmt) | shell | `/format` |
| `lint-check` | 运行 linter 并生成修复建议 | shell + filesystem(read) | `/lint` |
| `generate-changelog` | 从 Git 历史生成 changelog | git(read) | `/changelog` |
| `dependency-check` | 检查过时依赖和安全漏洞 | shell (只读) + network (仅 registry) | `/deps` |
| `project-init` | 从模板快速初始化项目 | filesystem(write) | `/init` |

---

## 8 Onboarding、主题、i18n

### 8.1 Onboarding 流程（首次使用引导）

#### 8.1.1 向导步骤

```
Step 1: 欢迎
  ┌───────────────────────────────────────┐
  │    🚀 欢迎使用 YourMate                │
  │                                       │
  │    你的本地 AI 编程助手                  │
  │    · 代码完全在本地，无需联网             │
  │    · 长期记忆，越用越懂你的项目            │
  │    · 从问询到全自动执行，自由切换          │
  │                                       │
  │    [开始设置]  [跳过，以后再说]           │
  └───────────────────────────────────────┘

Step 2: 模型配置
  ┌───────────────────────────────────────┐
  │    🤖 选择 AI 模型                     │
  │                                       │
  │    ○ 使用本地模型                       │
  │      [选择 Ollama/LM Studio 地址]       │
  │                                       │
  │    ○ 使用远程 API                       │
  │      [输入 API Key] [选择提供商]         │
  │                                       │
  │    ○ 稍后配置                          │
  │      (部分功能不可用)                    │
  │                                       │
  │    [上一步]  [下一步]                    │
  └───────────────────────────────────────┘

Step 3: 打开第一个项目
  ┌───────────────────────────────────────┐
  │    📂 打开你的项目                      │
  │                                       │
  │    [选择本地目录]  [克隆 Git 仓库]       │
  │                                       │
  │    最近项目: (空)                       │
  │                                       │
  │    [跳过]  [进入主页]                    │
  └───────────────────────────────────────┘

Step 4: 简介模式（可选）
  ┌───────────────────────────────────────┐
  │    💡 了解一下 YourMate                │
  │                                       │
  │    项目页面有三个区域：                   │
  │    ← 左侧: 会话和历史记忆                │
  │    ↕ 中间: 聊天区 / 输入命令             │
  │    → 右侧: 文件树 / 变更 / Git           │
  │                                       │
  │    试试输入: "这个项目是做什么的？"        │
  │                                       │
  │    [我知道了]                            │
  └───────────────────────────────────────┘
```

#### 8.1.2 Onboarding 触发条件

| 条件 | 行为 |
|------|------|
| 首次启动（无 `settings.json`） | 强制显示 Step 1-2；Step 3-4 可选跳过 |
| 已配置模型但无项目 | 启动后显示主页 + 空状态引导（"打开第一个项目"） |
| 正常用户 | 不显示任何引导，直接进入上次会话 |
| 用户手动触发 | 帮助菜单 → "重新运行引导"；或 `/onboarding` 命令 |

### 8.2 主题系统

#### 8.2.1 设计原则

- 使用 CSS 自定义属性（CSS Variables）实现主题切换，无需重渲染组件树
- 所有组件只引用语义 Token，不硬编码颜色
- 支持暗色 / 亮色两种内置主题，扩展点支持第三方主题

#### 8.2.2 主题 Token 层级

```
Layer 1: 基础色彩 (Primitive Tokens)
  定义原始色值，不直接使用
  --color-slate-50: #f8fafc;
  --color-slate-900: #0f172a;
  --color-blue-500: #3b82f6;
  --color-red-500: #ef4444;
  ...

Layer 2: 语义 Token (Semantic Tokens) ← 组件只引用这一层
  --bg-primary: var(--color-slate-50);           // 主背景
  --bg-secondary: var(--color-white);            // 卡片/面板背景
  --bg-tertiary: var(--color-slate-100);         // 悬停/选中
  --text-primary: var(--color-slate-900);        // 主文字
  --text-secondary: var(--color-slate-500);      // 辅助文字
  --text-link: var(--color-blue-600);            // 链接
  --border-primary: var(--color-slate-200);      // 边框
  --accent-primary: var(--color-blue-500);       // 主题色
  --accent-danger: var(--color-red-500);         // 危险操作
  --accent-success: var(--color-green-500);      // 成功
  --accent-warning: var(--color-amber-500);      // 警告
  --diff-added-bg: var(--color-green-100);       // diff 新增行
  --diff-removed-bg: var(--color-red-100);       // diff 删除行
  --syntax-keyword: var(--color-purple-500);     // 代码语法高亮
  --syntax-string: var(--color-green-600);
  --syntax-comment: var(--color-slate-400);
  ...

Layer 3: 组件 Token (Component Tokens) ← 可选，仅复杂组件使用
  --chat-bubble-user-bg: var(--bg-tertiary);
  --chat-bubble-ai-bg: transparent;
  --sidebar-width: 280px;
  --toolbar-height: 48px;
```

#### 8.2.3 主题切换实现

```typescript
// 主题定义 (themes.ts)
interface Theme {
  name: string;
  mode: 'light' | 'dark';
  tokens: Record<string, string>;  // CSS 变量 override
}

// 系统自动检测
const prefersDark = window.matchMedia('(prefers-color-scheme: dark)');

// 用户手选优先级 > 系统检测 > 默认亮色
// 值写入 settings.json → 所有窗口同步
```

CSS 变量切换：

```css
:root {
  /* 默认亮色 */
  --bg-primary: #f8fafc;
  --text-primary: #0f172a;
  /* ... 所有语义 Token */
}

:root[data-theme="dark"] {
  --bg-primary: #0f172a;
  --text-primary: #f1f5f9;
  /* ... */
}

/* 组件从不引用具体颜色 */
.message-content {
  color: var(--text-primary);
  background: var(--bg-secondary);
}
```

#### 8.2.4 扩展点

- 第三方主题通过 CSS 文件导入，覆盖语义 Token
- 用户可将自定义 Token 写入 `~/.YourMate/themes/my-theme.css`
- MVP 仅内置暗色 / 亮色，第三方主题支持排入 Phase 6

### 8.3 国际化 (i18n)

#### 8.3.1 架构选型：key-based, 编译时检查

```
locales/
├── en/
│   ├── common.json        # 共享字符串
│   ├── chat.json          # 聊天相关
│   ├── settings.json      # 设置页
│   ├── errors.json        # 错误提示
│   ├── update.json        # 更新相关
│   └── onboarding.json    # 引导流程
├── zh-CN/
│   ├── common.json
│   ├── chat.json
│   ├── settings.json
│   ├── errors.json
│   ├── update.json
│   └── onboarding.json
└── schema.json            # JSON Schema 校验所有 locale 键一致
```

#### 8.3.2 语言检测与切换

```
语言优先级（高→低）：
  1. 用户手动选择（设置 → 外观 → 语言）→ 写入 settings.json 的 "locale" 字段
  2. 系统语言（首次启动时自动检测）→ navigator.language / os locale
  3. 默认回退: en

切换行为:
  - 下拉选择即生效，无需重启
  - 所有 UI 组件从 i18n context 重新读取当前语言
  - Rust 端错误码由前端映射为本地化文案
  - 切换语言后聊天历史和 diff 内容不变（仅 UI 文案切换）
```

```typescript
// locale 自动检测 + 切换
const LOCALE_MAP: Record<string, string> = {
  'zh': 'zh-CN',
  'zh-CN': 'zh-CN',
  'zh-TW': 'zh-CN',   // MVP 阶段繁体也用简体
  'en': 'en',
  'en-US': 'en',
  'en-GB': 'en',
};

function detectLocale(): string {
  // 1. 用户手选优先生效
  const saved = settings.get('locale');
  if (saved && ['en', 'zh-CN'].includes(saved)) return saved;

  // 2. 系统语言检测
  for (const lang of navigator.languages) {
    const mapped = LOCALE_MAP[lang];
    if (mapped) return mapped;
  }

  // 3. 回退
  return 'en';
}
```

#### 8.3.3 翻译 Key 命名规范

```jsonc
// en/common.json
{
  "app.name": "YourMate",
  "button.save": "Save",
  "button.cancel": "Cancel",
  "button.retry": "Retry",
  "label.loading": "Loading...",
  "label.empty": "Nothing here yet",
  "time.seconds": "{n}s",
  "time.minutes": "{n}m"
}

// en/chat.json
{
  "chat.input.placeholder": "Ask anything about your project...",
  "chat.mode.ask": "Assistant",
  "chat.mode.plan": "Plan",
  "chat.mode.edit": "Edit",
  "chat.mode.auto": "Auto",
  "chat.error.timeout": "Model response timed out. {retry_link}",
  "chat.error.rate_limit": "Rate limited. Retry in {n}s."
}

// en/settings.json
{
  "settings.appearance": "Appearance",
  "settings.language": "Language",
  "settings.language.en": "English",
  "settings.language.zh-CN": "简体中文",
  "settings.theme": "Theme",
  "settings.theme.light": "Light",
  "settings.theme.dark": "Dark",
  "settings.theme.system": "Follow System",
  "settings.fontSize": "Font Size",
  "settings.editorFont": "Editor Font"
}

// en/update.json
{
  "update.newVersion": "New Version Available",
  "update.currentVersion": "Current version",
  "update.latestVersion": "Latest version",
  "update.downloadSize": "Download size",
  "update.checking": "Checking for updates...",
  "update.downloading": "Downloading update...",
  "update.downloaded": "Download complete",
  "update.installNow": "Restart & Install",
  "update.installLater": "Install Later",
  "update.remindLater": "Remind Later",
  "update.skipVersion": "Skip This Version",
  "update.failed": "Update Failed",
  "update.rollbackSuccess": "Rolled back to {version}"
}
```

#### 8.3.4 React 端用法

```typescript
import { useTranslation } from '@/i18n';

function ChatInput() {
  const { t } = useTranslation('chat');
  return <textarea placeholder={t('chat.input.placeholder')} />;
}

// 设置页语言切换下拉
function LanguageSelector() {
  const { locale, setLocale } = useLocale();
  return (
    <select value={locale} onChange={e => setLocale(e.target.value)}>
      <option value="en">English</option>
      <option value="zh-CN">简体中文</option>
    </select>
  );
}
```

编译时检查：CI 中运行 `locale-check` 脚本，验证所有语言的 key 完全一致（用 `schema.json` 作参考 Schema）。

#### 8.3.5 各阶段 i18n 范围

| 阶段 | 范围 | 语言 |
|------|------|------|
| Phase 0-2 | 硬编码英文 | 1 种 |
| Phase 3-5 | i18n 基础设施 + 英文 + 简体中文 | 2 种 |
| Phase 6+ | 社区贡献更多语言 | N 种 |

> **原则**：所有用户可见字符串必须在源码中以 key 形式引用，不可硬编码。Phase 0-2 期间允许只有 en locale，但 key 体系不变。

#### 8.3.6 Rust 端日志/错误消息

Rust 端不直接面向用户输出自然语言，仅输出结构化错误码和英文日志。前端负责将错误码映射为本地化文案。

```rust
// ❌ 不推荐：Rust 端硬编码用户可见文案
bail!("文件未找到: {}", path)

// ✅ 推荐：返回结构化错误码
Err(AppError::FileNotFound { path: path.clone() })
```

```jsonc
// 前端 errCode → 本地化文案映射
// en/errors.json
{
  "error.FILE_NOT_FOUND": "File not found: {path}",
  "error.PERMISSION_DENIED": "Permission denied: {path}",
  "error.SANDBOX_ESCAPE": "Command attempted to access restricted area",
  "error.LLM_TIMEOUT": "Model response timed out"
}

// zh-CN/errors.json
{
  "error.FILE_NOT_FOUND": "文件未找到：{path}",
  "error.PERMISSION_DENIED": "权限不足：{path}",
  "error.SANDBOX_ESCAPE": "命令试图访问受限区域",
  "error.LLM_TIMEOUT": "模型响应超时"
}
```

---

## 9 AI Loop 工具目录

每个工具以独立模块实现，遵循统一接口规范。以下按层分类逐个定义。

### 9.0 统一接口规范

所有工具实现以下 Rust trait：

```rust
/// 每个工具必须实现的基础 trait
pub trait Tool: Send + Sync {
    /// 工具唯一标识
    fn name(&self) -> &'static str;

    /// 功能描述（供 AI 选工具时参考）
    fn description(&self) -> &'static str;

    /// 参数 JSON Schema
    fn input_schema(&self) -> serde_json::Value;

    /// 执行（dry_run=true 时只模拟，不产生副作用）
    fn execute(&self, args: Value, ctx: &ToolContext, dry_run: bool)
        -> Result<ToolOutput, ToolError>;

    /// 回滚此次调用（如果支持）
    fn rollback(&self, execution_id: &str) -> Result<(), ToolError> {
        Err(ToolError::NotSupported)
    }

    /// 是否需要审计记录
    fn auditable(&self) -> bool { true }
}

/// 工具执行上下文
pub struct ToolContext {
    pub project_root: PathBuf,
    pub session_id: String,
    pub sandbox: Option<SandboxHandle>,
    pub network_allowed: bool,
}

/// 工具执行结果
pub struct ToolOutput {
    pub success: bool,
    pub data: Value,            // 结构化输出
    pub human_readable: String, // 给 AI 看的自然语言摘要
    pub execution_id: String,   // 用于回滚追踪
    pub artifacts: Vec<Artifact>, // 关联产物 (diff/changes)
}
```

各工具的通用属性汇总：

| 属性 | 说明 |
|------|------|
| **Name** | 工具名，AI 通过此名选择调用 |
| **Input** | JSON Schema，AI 按此构造参数 |
| **Output** | `ToolOutput { data, human_readable, artifacts }` |
| **Auditable** | 文件写入 / 命令执行类默认 `true`，只读类默认 `false` |
| **Dry-run** | 副作用工具支持，返回预期结果但不实际执行 |
| **Rollback** | 写入类工具支持，通过 `execution_id` 回滚 |

---

### 9.1 文件系统工具

#### read_file

| 属性 | 内容 |
|------|------|
| **功能** | 读取项目文件内容，支持行范围 |
| **Input** | `{ "path": string, "offset"?: int, "limit"?: int }` |
| **Output** | `{ "content": string, "total_lines": int, "path": string }` |
| **Auditable** | 否（只读） |
| **Dry-run** | 不适用 |
| **Rollback** | 不适用 |
| **调用模式** | ask / plan / edit / auto |
| **备注** | 自动排除 node_modules/.git；超出 2000 行时截断并标注 |

#### write_file

| 属性 | 内容 |
|------|------|
| **功能** | 创建或覆盖文件 |
| **Input** | `{ "path": string, "content": string }` |
| **Output** | `{ "path": string, "bytes_written": int, "hash": string }` |
| **Auditable** | 是 |
| **Dry-run** | 是（返回将写入的内容预览） |
| **Rollback** | 是（备份原文件 → `execution_id`） |
| **调用模式** | edit / auto |
| **安全约束** | path 必须在项目目录内；禁止覆盖 .git 目录内文件 |

#### edit_file

| 属性 | 内容 |
|------|------|
| **功能** | 基于精确字符串匹配的局部替换（同 Claude Code 的 Edit） |
| **Input** | `{ "path": string, "old_string": string, "new_string": string, "replace_all"?: bool }` |
| **Output** | `{ "path": string, "replaced": int, "diff": string }` |
| **Auditable** | 是 |
| **Dry-run** | 是（返回将要生成的 diff） |
| **Rollback** | 是 |
| **调用模式** | edit / auto |
| **备注** | old_string 必须唯一匹配（replace_all=false 时）；匹配失败返回错误并提示 |

#### search_replace

| 属性 | 内容 |
|------|------|
| **功能** | 基于正则表达式的批量搜索替换 |
| **Input** | `{ "path": string, "pattern": string, "replacement": string, "glob"?: string }` |
| **Output** | `{ "path": string, "matches": int, "replaced": int, "diff": string }` |
| **Auditable** | 是 |
| **Dry-run** | 是（列出匹配位置，不实际替换） |
| **Rollback** | 是 |
| **调用模式** | edit / auto |

#### glob

| 属性 | 内容 |
|------|------|
| **功能** | 文件名模式匹配，返回匹配的文件列表（按修改时间排序） |
| **Input** | `{ "pattern": string, "path"?: string }` |
| **Output** | `{ "files": [{ "path": string, "size": int, "modified": string }] }` |
| **Auditable** | 否（只读） |
| **调用模式** | ask / plan / edit / auto |

#### grep

| 属性 | 内容 |
|------|------|
| **功能** | 基于 ripgrep 的内容搜索，支持正则、文件类型过滤、上下文行 |
| **Input** | `{ "pattern": string, "path"?: string, "glob"?: string, "type"?: string, "context"?: int, "multiline"?: bool, "head_limit"?: int }` |
| **Output** | `{ "matches": [{ "file": string, "line": int, "content": string }], "total_count": int }` |
| **Auditable** | 否（只读） |
| **调用模式** | ask / plan / edit / auto |

#### list_directory

| 属性 | 内容 |
|------|------|
| **功能** | 列出目录内容 |
| **Input** | `{ "path": string, "depth"?: int, "show_hidden"?: bool }` |
| **Output** | `{ "entries": [{ "name": string, "type": "file"|"dir"|"symlink", "size": int }] }` |
| **Auditable** | 否（只读） |
| **调用模式** | ask / plan / edit / auto |

#### change_dir

| 属性 | 内容 |
|------|------|
| **功能** | 切换当前工作目录（影响后续相对路径解析） |
| **Input** | `{ "path": string }` |
| **Output** | `{ "cwd": string }` |
| **Auditable** | 否 |
| **调用模式** | edit / auto |
| **备注** | 仅限项目目录内切换；此目录绑定到当前 tool call 会话 |

---

### 9.2 Shell 与命令工具

#### bash

| 属性 | 内容 |
|------|------|
| **功能** | 在沙箱中执行 shell 命令 |
| **Input** | `{ "command": string, "cwd"?: string, "timeout_ms"?: int, "env"?: object }` |
| **Output** | `{ "exit_code": int, "stdout": string, "stderr": string, "duration_ms": int }` |
| **Auditable** | 是（全量记录到 audit_log） |
| **Dry-run** | 是（仅做安全检查，不执行） |
| **Rollback** | 否（命令副作用不可逆） |
| **调用模式** | edit / auto |
| **安全约束** | 沙箱隔离（见 3.5.2 Sandbox 行）；高危模式检测；默认无网络；超时 120s |

沙箱规则：
- 限制项目目录外访问
- 禁止 `sudo`、`su`、`chown`
- 检测 `rm -rf /`、`curl | sh`、`eval` 等高危模式并拦截
- 网络默认阻止，需用户配置白名单

---

### 9.3 网络工具

#### web_search

| 属性 | 内容 |
|------|------|
| **功能** | 执行网络搜索，返回结果摘要 |
| **Input** | `{ "query": string, "allowed_domains"?: string[], "blocked_domains"?: string[] }` |
| **Output** | `{ "results": [{ "title": string, "url": string, "snippet": string }] }` |
| **Auditable** | 否 |
| **调用模式** | ask / plan / edit / auto |
| **约束** | 需 network 权限；仅 HTTPS；结果缓存 15 分钟 |

#### web_fetch

| 属性 | 内容 |
|------|------|
| **功能** | 获取 URL 内容并转换为 Markdown |
| **Input** | `{ "url": string, "prompt"?: string }` |
| **Output** | `{ "markdown": string, "title": string, "url": string }` |
| **Auditable** | 否 |
| **调用模式** | ask / plan / edit / auto |
| **约束** | 需 network 权限；HTTP 自动升级为 HTTPS；结果缓存 15 分钟 |

---

### 9.4 代码图谱工具（语言感知）

代码图谱工具基于 Tree-sitter 解析 AST 实现，MVP 阶段支持 TypeScript/JavaScript、Python、Rust，后续扩展更多语言。

底层依赖：
- Tree-sitter（语法解析）
- 项目级索引（SQLite 存储符号位置，增量更新）

#### list_symbols

| 属性 | 内容 |
|------|------|
| **功能** | 列出指定范围内的所有符号（类、函数、变量、接口、类型等） |
| **Input** | `{ "path": string, "kind"?: "class"|"function"|"variable"|"interface"|"type"|"all" }` |
| **Output** | `{ "symbols": [{ "name": string, "kind": string, "line": int, "signature": string }] }` |
| **调用模式** | ask / plan / edit / auto |
| **备注** | 大文件（> 5000 行）时按 top-level 过滤 |

#### read_symbol

| 属性 | 内容 |
|------|------|
| **功能** | 读取符号的完整定义（包含源码、注释、类型签名） |
| **Input** | `{ "symbol": string, "path": string }` |
| **Output** | `{ "name": string, "kind": string, "signature": string, "body": string, "doc_comment": string|null, "location": { "path": string, "line": int } }` |
| **调用模式** | ask / plan / edit / auto |

#### find_references

| 属性 | 内容 |
|------|------|
| **功能** | 查找符号的所有引用位置 |
| **Input** | `{ "symbol": string, "path": string, "include_definition"?: bool }` |
| **Output** | `{ "references": [{ "path": string, "line": int, "context": string }] }` |
| **调用模式** | ask / plan / edit |
| **备注** | 基于 Tree-sitter 语义分析，非文本 grep |

#### trace_callers

| 属性 | 内容 |
|------|------|
| **功能** | 追踪谁调用了指定的函数/方法（向上追溯） |
| **Input** | `{ "symbol": string, "path": string, "depth"?: int }` |
| **Output** | `{ "callers": [{ "symbol": string, "path": string, "line": int, "callers": [...] }] }` |
| **调用模式** | ask / plan |
| **备注** | 默认 depth=1（仅直接调用者）；depth=0 不限深度 |

#### trace_callees

| 属性 | 内容 |
|------|------|
| **功能** | 追踪指定函数调用了哪些函数（向下追溯） |
| **Input** | `{ "symbol": string, "path": string, "depth"?: int }` |
| **Output** | `{ "callees": [{ "symbol": string, "path": string, "line": int, "callees": [...] }] }` |
| **调用模式** | ask / plan |

#### trace_chain

| 属性 | 内容 |
|------|------|
| **功能** | 构建两个符号之间的调用路径链 |
| **Input** | `{ "from_symbol": string, "to_symbol": string, "max_depth"?: int }` |
| **Output** | `{ "chains": [[{ "symbol": string, "path": string, "line": int }]], "shortest_length": int }` |
| **调用模式** | ask / plan |
| **备注** | BFS 搜索；max_depth 默认 10；返回所有路径（最多 20 条），按长度排序 |

#### file_deps

| 属性 | 内容 |
|------|------|
| **功能** | 分析指定文件的 import / require 依赖关系 |
| **Input** | `{ "path": string, "direction": "imports"|"imported_by"|"both" }` |
| **Output** | `{ "dependencies": [{ "path": string, "type": "internal"|"external"|"stdlib", "symbols": string[] }] }` |
| **调用模式** | ask / plan / edit |

#### blast_radius

| 属性 | 内容 |
|------|------|
| **功能** | 变更爆炸半径分析：修改某个符号/文件后，受影响的所有文件和符号 |
| **Input** | `{ "path": string, "symbol"?: string }` |
| **Output** | `{ "affected_files": [{ "path": string, "reason": string, "affected_symbols": string[] }], "risk_level": "low"|"medium"|"high", "summary": string }` |
| **调用模式** | plan / edit |
| **备注** | 综合 file_deps + find_references + trace_callers；risk_level 由受影响文件数和距离计算 |

---

### 9.5 变更与 Diff 工具

#### CodeEditor (apply_patch)

| 属性 | 内容 |
|------|------|
| **功能** | 将 unified diff 补丁应用到工作区文件 |
| **Input** | `{ "patch": string, "target"?: string[] }` |
| **Output** | `{ "applied": [{ "path": string, "status": "ok"|"conflict"|"skipped" }], "failed": int, "summary": string }` |
| **Auditable** | 是 |
| **Dry-run** | 是（返回 "would apply to N files"） |
| **Rollback** | 是（通过 git_snapshot） |
| **调用模式** | edit / auto |
| **约束** | 应用前检查文件 hash，冲突时弹到前端 |

#### DiffViewer

| 属性 | 内容 |
|------|------|
| **功能** | 生成指定文件/目录的工作区与 HEAD 的 diff |
| **Input** | `{ "path"?: string, "staged"?: bool, "context_lines"?: int }` |
| **Output** | `{ "diff": string, "files": string[], "stats": { "added": int, "removed": int, "changed": int } }` |
| **Auditable** | 否（只读） |
| **调用模式** | ask / plan / edit / auto |

---

### 9.6 执行与验证工具

#### TestRunner

| 属性 | 内容 |
|------|------|
| **功能** | 自动发现项目测试框架，运行测试并解析结果 |
| **Input** | `{ "scope"?: "all"|"file"|"function", "path"?: string, "test_name"?: string, "timeout_ms"?: int }` |
| **Output** | `{ "passed": int, "failed": int, "skipped": int, "duration_ms": int, "failures": [{ "name": string, "message": string, "file": string, "line": int }] }` |
| **Auditable** | 否 |
| **调用模式** | edit / auto（执行后自动反馈给 AI 分析） |
| **框架检测** | Jest / Vitest / pytest / cargo test / go test |
| **备注** | 失败时自动摘要 top 5 条 failure 给 AI |

#### LinterAnalyzer

| 属性 | 内容 |
|------|------|
| **功能** | 对指定文件运行 linter，返回结构化问题列表 |
| **Input** | `{ "path": string, "linter"?: string }` |
| **Output** | `{ "issues": [{ "file": string, "line": int, "severity": "error"|"warning"|"info", "rule": string, "message": string, "fixable": bool }] }` |
| **Auditable** | 否 |
| **调用模式** | edit / auto |
| **支持 linter** | eslint / ruff / clippy / shellcheck（自动检测） |

#### SandboxExecutor

| 属性 | 内容 |
|------|------|
| **功能** | 在隔离沙箱中执行任意命令（bash 的安全封装） |
| **Input** | `{ "command": string, "cwd": string, "env"?: object, "timeout_ms"?: int }` |
| **Output** | `{ "exit_code": int, "stdout": string, "stderr": string, "duration_ms": int, "truncated": bool }` |
| **Auditable** | 是 |
| **调用模式** | edit / auto |
| **备注** | 输出超过 100KB 时截断；stderr 始终完整保留 |

---

### 9.7 Git 工具

#### GitManager

对外暴露为 6 个子操作，统一入口：

| 子操作 | 功能 | Input | Auditable |
|--------|------|-------|-----------|
| `status` | 获取工作区状态 | `{ "path"?: string }` | 否 |
| `diff` | 生成 diff | `{ "path"?: string, "staged"?: bool, "target"?: string }` | 否 |
| `commit` | 创建提交 | `{ "message": string, "files"?: string[] }` | 是 |
| `branch` | 分支操作 | `{ "action": "list"|"create"|"switch"|"delete", "name"?: string }` | 部分 |
| `stash` | 暂存/恢复 | `{ "action": "push"|"pop"|"list" }` | 是 |
| `log` | 提交历史 | `{ "count"?: int, "path"?: string }` | 否 |

**调用模式**：ask / plan / edit / auto

**安全约束**：
- `commit` 在 Auto 模式静默执行，Ask 模式需确认
- `push` 操作始终需用户确认
- 强制 push（`--force`）被拦截

---

### 9.8 记忆与检索工具

#### Retriever

| 属性 | 内容 |
|------|------|
| **功能** | 多策略融合检索：语义相似度（FAISS）+ 关键词（FTS）+ 时间衰减 + 重要性加权 |
| **Input** | `{ "query": string, "types"?: string[], "top_k"?: int, "min_importance"?: float, "recency_bias"?: float }` |
| **Output** | `{ "results": [{ "id": string, "content": string, "type": string, "score": float, "source_session": string }] }` |
| **调用模式** | ask / plan / edit / auto |
| **备注** | AI Loop 执行前自动调用，注入到 prompt 上下文 |

融合公式：

```
score = α × cosine_sim(query_embedding, memory_embedding)
      + β × (1 / (1 + days_since_last_access))
      + γ × importance
```

默认权重：α=0.6, β=0.25, γ=0.15

#### MemoryCompressor

| 属性 | 内容 |
|------|------|
| **功能** | 定期聚合相似记忆 → 生成摘要 → 更新版本链 → 删除冗余 |
| **Input** | `{ "project_id": string, "threshold"?: int }` |
| **Output** | `{ "compressed": int, "deleted": int, "new_summaries": int }` |
| **Auditable** | 是 |
| **触发时机** | 记忆数 > 1000 时自动触发；或用户手动 `/compress-memory` |
| **压缩策略** | 同 type + 同 source_session + cosine_sim > 0.85 → 合并摘要 |

---

### 9.9 编排工具

#### PlanGenerator

| 属性 | 内容 |
|------|------|
| **功能** | 根据用户需求生成结构化执行计划（Markdown 文档） |
| **Input** | `{ "requirement": string, "context"?: string, "constraints"?: string[] }` |
| **Output** | `{ "plan": string, "steps": [{ "order": int, "action": string, "tool": string, "args_preview": object, "expected_result": string }] }` |
| **调用模式** | plan / ask |

---

### 9.10 工具使用矩阵

```
              ask    plan   edit   auto
read_file      ✓      ✓      ✓      ✓
write_file     -      -      ✓      ✓
edit_file      -      -      ✓      ✓
search_replace -      -      ✓      ✓
glob           ✓      ✓      ✓      ✓
grep           ✓      ✓      ✓      ✓
list_directory ✓      ✓      ✓      ✓
change_dir     -      -      ✓      ✓
bash           -      -      ✓      ✓
web_search     ✓      ✓      ✓      ✓
web_fetch      ✓      ✓      ✓      ✓
list_symbols   ✓      ✓      ✓      ✓
read_symbol    ✓      ✓      ✓      ✓
find_references ✓     ✓      ✓      -
trace_callers  ✓      ✓      -      -
trace_callees  ✓      ✓      -      -
trace_chain    ✓      ✓      -      -
file_deps      ✓      ✓      ✓      -
blast_radius   -      ✓      ✓      -
apply_patch    -      -      ✓      ✓
diff_viewer    ✓      ✓      ✓      ✓
test_runner    -      -      ✓      ✓
linter         -      -      ✓      ✓
sandbox_exec   -      -      ✓      ✓
git_status     ✓      ✓      ✓      ✓
git_diff       ✓      ✓      ✓      ✓
git_commit     -      -      ✓      ✓
git_branch     ✓      ✓      ✓      ✓
git_stash      -      -      ✓      ✓
git_log        ✓      ✓      ✓      ✓
retrieve       ✓      ✓      ✓      ✓
compress       -      -      -      -
plan_generate  ✓      ✓      -      -
```

---

## 附录：后续可产出文档

- **完整的 API 设计文档**
- **AI Loop 序列图**
- **Figma 原型结构图**
- **MVP 验收标准文档**
- **Skill 插件开发指南**
- **代码图谱 Tree-sitter 集成方案**
